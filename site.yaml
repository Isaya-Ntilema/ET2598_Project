---
- hosts: webservers
  gather_facts: yes
  become: yes
  tasks:
    - name: Updating the repositories
      apt:
        update_cache: yes
        cache_valid_time: 3600
      
    - name: Installing Python3-pip
      apt:
         name: python3-pip
         state: present
         
    - name: Installing flask 
      ansible.builtin.pip:
        name: flask
        executable: pip3
        state: latest

    - name: Uploading application to the webservers
      copy:
        src: ./service.py
        dest: /home/ubuntu
        owner: ubuntu
        group: ubuntu
        mode: 0644

    - name: Running the application
      shell: python3 /home/ubuntu/service.py
      async: 3600
      poll: 0
      
    - name: Installing the snmpdaemon
      apt:
              name: snmpd
              state: latest
    
    - name: Copying the snmpd config file
      template:
              src: snmpd.conf.j2
              dest: "/etc/snmp/snmpd.conf"
    - name: Running the snmpdaemon
      service:
              name: snmpd
              state: restarted    

- hosts: haproxy
  gather_facts: yes
  become: yes
  tasks:
    - name: Updating repositories
      apt:
        update_cache: yes
        cache_valid_time: 3600
    - name: Install HAproxy
      apt:
        name: haproxy
        state: present
    - name: Uploading haproxy config file
      template:
        src: ./haproxy.cfg.j2
        dest: /etc/haproxy/haproxy.cfg
    - name: Installing snmp and snmp-mibs-downloader
      apt: 
        update_cache: yes
        name: [ 'snmp', 'snmp-mibs-downloader' ] 
        state: latest
    - name: Restart HAProxy
      service:
        name: haproxy
        state: restarted
